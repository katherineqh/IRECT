{

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "data": "json-server --port 12345 --watch ./data/db.json"
  },
  "dependencies": {
    "scss": "^0.2.4"
  }
}
